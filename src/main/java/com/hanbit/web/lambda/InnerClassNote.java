package com.hanbit.web.lambda;

public class InnerClassNote {
	/*
	 내부클래스
	 -클래스내부에 선언된 틀래스
	 -사용하는 경우는 두 클래스 관계가 긴밀하여
	 -멤버들간에 서로 쉽게 접근시키도록 하기위함
	 -종류
	 --인스턴스 클래스(instance class)
	 --스태틱클래스(static class)
	 --로컬클래스(local class)
	 --익명클래스(anonymous class)
	 * **/

}
